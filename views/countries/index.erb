<p>Countries</p>

<p class="btn--wrapper">
  <a class="btn btn--action" href="/countries/new">Add a new country</a>
</p>s

<ul>
  <% for country in @countries %>
  <li>
  <p> <%= country.name %>
   <% if country.visited == 't' %>
      <img src="green_tick.png" alt="green_tick">
    <% else %>
      <img src="red_cross.png" alt="red_cross">
      </p>
   <% end %>
  </li>
  <% end %>
</ul>
